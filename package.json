{
  "name": "timedoor-eslint-plugin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm build:plugin && pnpm build:config",
    "build:config": "cd packages/eslint-config-nuxt && npx tsc --build",
    "build:plugin": "cd packages/eslint-plugin-nuxt && npx tsc --build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "eslint": "^10.0.0"
  },
  "devDependencies": {
    "@types/eslint": "^9.6.1",
    "@types/node": "^25.3.0",
    "@typescript-eslint/types": "^8.56.0",
    "typescript": "^5.9.3"
  }
}
